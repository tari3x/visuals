(executables (
  (names (index index_rain index_load index_server))
  (libraries (
    base
    lwt
    js_of_ocaml
    js_of_ocaml-lwt
    visuals
    remote
    grid_lib
  ))
  (js_of_ocaml (
    (flags (+weak.js))
  ))
  (preprocess (pps (
    ppx_jane
    js_of_ocaml-ppx
  )))
))
