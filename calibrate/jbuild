(executables (
  (names (index))
  (libraries (
    base
    lwt
    js_of_ocaml
    js_of_ocaml-lwt
    visuals
  ))
  (js_of_ocaml (
    (flags (+weak.js))
  ))
  (preprocess (pps (
    ppx_jane
    js_of_ocaml-ppx
  )))
))
