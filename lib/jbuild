(library (
  (name visuals)
  (libraries (
    base
    lwt
    js_of_ocaml
    js_of_ocaml-lwt
    common_lib
  ))
  (preprocess (pps (
    ppx_jane
    js_of_ocaml-ppx
  )))
  (js_of_ocaml (
    (javascript_files (js_std.js))
  ))
))
